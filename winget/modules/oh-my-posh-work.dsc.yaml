# yaml-language-server: $schema=https://aka.ms/dsc/schemas/v3/bundled/config/document.json
# Work Oh My Posh deployment resource.
$schema: https://aka.ms/dsc/schemas/v3/bundled/config/document.json
resources:
  - name: Deploy Oh My Posh config
    type: Microsoft.DSC.Transitional/PowerShellScript
    metadata:
      description: "Deploy Oh My Posh prompt theme configuration file"
      module: "Microsoft.DSC.Transitional"
    properties:
      input:
        gistBase: "https://gist.githubusercontent.com/matracey/f4c3a9194cd190a3b3de423aad5dc7e1/raw"
        fileName: "mojada.work.omp.json"
      testScript: |
        param($inputData)
        $profileDir = Split-Path $PROFILE.CurrentUserAllHosts
        $target = Join-Path $profileDir 'themes\mojada.omp.json'
        return (Test-Path $target)
      setScript: |
        param($inputData)
        $profileDir = Split-Path $PROFILE.CurrentUserAllHosts
        $target = Join-Path $profileDir 'themes\mojada.omp.json'
        $dir = Split-Path $target
        if (-not (Test-Path $dir)) { New-Item -ItemType Directory -Path $dir -Force | Out-Null }
        $url = "$($inputData.gistBase)/$($inputData.fileName)"
        Invoke-WebRequest -Uri $url -OutFile $target -UseBasicParsing
